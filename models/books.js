const mongoose = require('mongoose');

var bookSchema = new mongoose.Schema({
  id: String,
  image: String,
  title: String,
      "description": "Doraemon is the name of a robot cat that came from the future to help a boy named Nobita Nobi. Doraemon is about the life of
})

